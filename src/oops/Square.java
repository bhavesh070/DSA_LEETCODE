package PACKAGE_NAME.oops;

public class Square extends Shape{
    @Override
    public int area(int side) {
        return super.area(side);
    }

    @Override
    public int permieter(int side) {
        return super.permieter(side);
    }
}
